# p83 — Digimax / DSM Web Workspace

Everything related to the DSM ("digimax") website, consolidated 2026-07-13.

**Live site:** https://digimax-93q.pages.dev (Cloudflare Pages)
- `/` and `/services` → main DSM SPA ("DSM | Luxury Software Licensing Concierge")
- `/marketing/` → dsm-marketing sub-site

## What's here

| Path | What it is | Source |
|---|---|---|
| `/` (root) | Main DSM site — Vite + React + TS + Tailwind + shadcn. Serves `/` and `/services`. | Clone of `oyehroonn/digital-software-hub` (newest source, matches live deploy) |
| `public/models/` | 381 GLB 3D product-box models used by the storefront | Generated by `3dstuff/` pipeline |
| `dsm-marketing/` | Marketing sub-site (React + Vite), deployed at `/marketing/` | Same repo |
| `DSMAILABPAGES/` | Static AI Lab / case-study HTML pages | Same repo |
| `3dstuff/` | **GLB box generator**: `box.glb` (empty box), `products.xlsx` (15MB, embedded product images), `process_product.py` / `batch_process.py` (openpyxl + trimesh + PIL → textures images onto box.glb), `api.py` (Flask), `viewer.html`, `check_excel.py` | Copied from VPS `root@164.68.117.31:/root/3dstuff/` |
| `digitech/` | Old "DigiTech" static HTML template site (Services/about/blog/contact) | `Majboor/digitech` |
| `dsm-chat-widget-ai/` | AI chat widget for the DSM site | `Majboor/dsm-chat-widget-ai` |

## Related, not copied here

- `waleed_codes/p15` — older local workspace (DSM-REVAMPED-WEB) incl. `dsm-agentic` fork (origin `Majboor/dsm-agentic`, upstream `waleed-might-code/dsmonline`)
- VPS `root@164.68.117.31` (SSH key auth works): `/root/3dstuff/models/` (98MB generated output) and `/root/3dstuff/dsm-3d.zip` (110MB) left there
- Repo network: `oyehroonn/digital-software-hub` (current) ← `Majboor/dsm-agentic` ← `waleed-might-code/dsmonline`

## Next steps

1. `npm install` in root and `dsm-marketing/`, confirm both build (`npm run dev` / `npm run build`)
2. Decide the canonical git remote (digital-software-hub vs dsm-agentic) and push future work there
3. `pip install -r 3dstuff/requirements.txt` to run the box generator locally; regenerate/fix models flagged `_FIXED`/`_NEW`
4. Wire deploy: confirm how digimax-93q.pages.dev builds (root build + marketing build copied to `/marketing/`) and reproduce it in CI
5. Integrate `dsm-chat-widget-ai` into the main site if still wanted
