# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# DSM product DB dumps (large, from moodular drive)
3dstuff/db-dumps/
admin-app/node_modules/
admin-app/src-tauri/target/

# Env & secrets — ignore all real env files everywhere, keep *.example templates
.env
.env.local
.env.*.local
**/.env
**/.env.local
!.env.example
!.env.local.example
!**/*.env.example

# Admin app local config (holds secrets)
admin-app/config.json
**/config.local.json

# Python tooling junk
__pycache__/
**/__pycache__/
*.pyc
.vscode/

# Migrated WooCommerce customer PII — never in the public repo (lives only in the private admin repo)
admin-app/public/legacy/
woo-export/

# Admin app built into the store deploy (generated by build:admin)
public/admin/
